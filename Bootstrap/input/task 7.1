<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Bootstrap Typography Utilities</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>

  <div class="container mt-5">
    <!-- Large heading -->
    <h1 class="display-1">Display 1 Heading</h1>

    <!-- Lead paragraph -->
    <p class="lead">
      This is a lead paragraph — it stands out by using larger font size and lighter weight.
    </p>

    <!-- Muted text -->
    <p class="text-muted">
      This paragraph uses <code>text-muted</code> class to show secondary, less emphasized text.
    </p>

    <!-- Bold text -->
    <p class="fw-bold">
      This text is bold using <code>fw-bold</code> utility.
    </p>

    <!-- Italic text -->
    <p class="fst-italic">
      This text is italicized with <code>fst-italic</code>.
    </p>

    <!-- Underlined text -->
    <p class="text-decoration-underline">
      This text is underlined using <code>text-decoration-underline</code>.
    </p>

    <!-- Small text -->
    <p class="small">
      This is small text, often used for fine print or disclaimers.
    </p>

    <!-- Text alignment -->
    <p class="text-center">
      Center aligned text with <code>text-center</code>.
    </p>

    <p class="text-end">
      Right aligned text with <code>text-end</code>.
    </p>

    <!-- Text transform -->
    <p class="text-uppercase">
      This text is uppercase with <code>text-uppercase</code>.
    </p>

    <p class="text-lowercase">
      THIS TEXT IS LOWERCASE with <code>text-lowercase</code>.
    </p>

    <p class="text-capitalize">
      this text is capitalized with <code>text-capitalize</code>.
    </p>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
