<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Local Community Event Portal</title>

  <!-- Internal CSS -->
  <style>
    /* Reset Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* General body styling */
    body {
      background-color: #f0f8ff; /* Internal style for background */
      font-family: 'Roboto', sans-serif;
      line-height: 1.6;
      color: #333;
    }

    /* Header styles */
    #mainHeader {
      font-size: 2rem;
      text-align: center;
      margin-top: 20px;
      color: #004080;
    }

    /* Style for event card container */
    .eventCard {
      border: 1px solid #ccc;
      padding: 20px;
      margin: 20px auto;
      max-width: 500px;
      background-color: #ffffff;
      box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
    }

    /* Style for event headings */
    .eventCard h3 {
      margin-bottom: 10px;
      color: #006400;
    }

    /* Style for event description */
    .eventCard p {
      margin-bottom: 15px;
      font-size: 1rem;
    }

    /* Style for main CTA button */
    .cta-button {
      background-color: #007BFF;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1rem;
    }

    .cta-button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>

  <!-- Inline CSS: make heading red -->
  <h1 style="color: red;">Welcome to the Local Community Event Portal</h1>

  <!-- ID selector -->
  <h2 id="mainHeader">Upcoming Events</h2>

  <!-- Class selector -->
  <div class="eventCard">
    <h3>Farmers Market</h3>
    <p>Join us every Sunday for fresh local produce.</p>
    <button class="cta-button">Join Now</button>
  </div>

</body>
</html>
